# DraMark Language Specification

**Version**: 0.2.0 (pre-v1)  
**Date**: 2026-03-19  
**Status**: Official Release

## 目录

1. [概述](#1-概述)
2. [与 CommonMark 的关系](#2-与-commonmark-的关系)
3. [核心解析模型：二维状态机](#3-核心解析模型二维状态机)
4. [文档配置层 (Frontmatter)](#4-文档配置层-frontmatter)
5. [角色与台词](#5-角色与台词)
6. [场景动作与结构分隔](#6-场景动作与结构分隔)
7. [唱段与音乐容器](#7-唱段与音乐容器)
8. [技术提示标记](#8-技术提示标记)
9. [注释](#9-注释)
10. [译配与多语言模式](#10-译配与多语言模式)
11. [转义字符](#11-转义字符)
12. [解析器实现指引与边缘情况裁决 (Edge Cases)](#12-解析器实现指引与边缘情况裁决-edge-cases)
13. [显式退出机制快速参考表](#13-显式退出机制快速参考表)
14. [附录 A：输入法与编辑器体验优化 (IME Tricks)](#附录-a输入法与编辑器体验优化-ime-tricks)

## 1. 概述

DraMark 是一门专为戏剧、影视及音乐剧的剧本创作设计的纯文本标记语言。其核心目标为：

1. **少输入法切换 (Few IME Switch)**：核心符号 `@`, `%`, `=` 在中文键盘下可直接通过 Shift 键打出，保证编剧创作心流不断。
2. **状态机驱动 (State Machine Driven)**：摒弃繁琐的闭合标签，通过极简的上下文状态推导台词、唱段和舞台动作的作用域。
3. **原生译配支持 (Native Dual-track)**：内建强大的双语译配工作流，一份源文件可编译为演员本、导演本、工作本。
4. **技术与艺术解耦**：将舞台监督的 Cue 标记与角色演出文本彻底分离。

## 2. 与 CommonMark 的关系

DraMark 是 CommonMark 的超集方言。
DraMark 解析器在生成 AST 时，并不破坏 CommonMark 的基础区块（如段落、列表、引用块），而是通过**语义注入**的方式将它们归属于特定角色或动作节点。所有的 CommonMark 格式（如 `*斜体*`、`**加粗**`）在 DraMark 中完全保留。

YAML Frontmatter 在本规范中被定义为**文档配置层（Document Config Layer）**，用于为编辑器/渲染器/编译器提供配置数据；它不属于 DraMark 正文语法本体。

## 3. 核心解析模型：二维状态机

DraMark 的解析引擎基于一个**二维状态机（2D State Machine）**运行。解析器在任何时刻处于以下状态的交叉点：

- **维度一：表演状态 (Performance Context)**
  - `全局动作 (Global Action)`：默认状态。文本被解析为舞台指示或场景描述。
  - `角色状态 (Character)`：文本被解析为当前角色的台词。
- **维度二：音乐状态 (Musical Context)**
  - `对白模式 (Spoken)`：默认状态。正常的戏剧对白。
  - `唱段模式 (Sung)`：进入音乐剧曲目（Musical Number）状态。

状态的切换由核心指令（`@`, `---`, `#`, `$$`）驱动。详见后续章节。

## 4. 文档配置层 (Frontmatter)

文档可由 YAML Frontmatter 块开头，用于提供剧本配置数据。
该配置块在解析前优先提取，并交由上层系统（前端/渲染器/编译器）消费。
解析器应遵循“宽松 + 推荐字段”策略：未知字段允许透传。

```yaml
---
meta:
  title: 悲惨世界 (Les Misérables)
  locale: zh-CN
  version: 0.1.0
casting:
  characters:
    - name: 冉阿让
      actor: 张三
      mic: HM1
      aliases: [24601]
  groups:
    principals:
      members: [冉阿让]
translation:
  enabled: true
  source_lang: en
  target_lang: zh-CN
  render: bilingual # source | target | bilingual
tech:
  mics:
    - id: HM1
      label: Hamlet 主麦
      color: "#4B8BFF"
  sfx:
    - id: SND_THUNDER
      file: thunder.mp3
      desc: 雷声
  lx:
    - id: LX01
      desc: 冷白顶光
      color: "#E6EEFF"
  keywords:
    - token: blackout
      label: 黑场
      color: "#111111"
---
```

### 4.1 推荐配置命名空间

- `meta`：剧本元数据（标题、语言、版本、版权等）
- `casting`：角色清单与分组（角色 ID、展示名、别名、分组映射）
- `translation`：译配开关与渲染策略
- `tech`：技术资源字典（麦克风、音效、灯效、关键词与颜色）

### 4.2 解析器职责边界

- 解析器负责：
  - 提取 frontmatter 原文（供上层做归一化）
  - 提供最小可用元信息（例如 `translation.enabled`）
- 解析器不负责：
  - 严格业务 schema 校验
  - 消费端的呈现/编排策略
- 未知字段必须保留（forward-compatible）

### 4.3 `casting` 关联规则（推荐）

- `group` 只在 `casting.groups` 定义，作为分组单一事实来源（single source of truth）。
- `@角色` 行不携带分组字段；正文仅声明“当前发言者标识”。
- `casting.characters[]` 的最小推荐字段为：
  - `name`（建议唯一；默认检索键）
  - `id?`（仅在重名或跨系统关联时建议提供）
  - `actor?`（演员展示名）
  - `mic?`（角色默认麦克风 ID，建议引用 `tech.mics[].id`）
  - `aliases?`
- `casting.groups.<groupName>.members[]` 可写 `name` 或 `id`；默认按 `name` 匹配，若成员值命中某个 `id` 则按 `id` 优先。
- 当出现重名且未提供 `id` 时，消费端应给 warning，但不阻断解析。
- 兼容优先原则：非重名场景下，不要求提供 `id`；`@角色` 匹配与 group 成员匹配都可直接按 `name` 完成。
- `actor` 与 `mic` 均为可选字段，缺省时不影响解析。

### 4.4 `tech` 最小可执行模型（推荐）

`tech` 推荐包含四类字典：`mics`、`sfx`、`lx`、`keywords`。每类内 `id` 应唯一；未知字段允许透传。

- `tech.mics[]`
  - `id`（必填）
  - `label`（必填）
  - `color?`, `tags?`, `enabled?`
- `tech.sfx[]`
  - `id`（必填）
  - `file`（必填）
  - `desc?`, `color?`, `tags?`, `enabled?`
- `tech.lx[]`
  - `id`（必填）
  - `desc`（必填）
  - `color?`, `tags?`, `enabled?`
- `tech.keywords[]`
  - `token`（必填）
  - `label`（必填）
  - `color?`, `tags?`, `enabled?`

### 4.5 换麦扩展配置（app/扩展层，推荐）

为支持“演员麦克风分配/换麦”而不硬编码单一语言关键词，推荐在 `tech` 下增加可选配置：

- `tech.defaultMicBehavior?`
  - 用于定义默认目标解析行为（如省略目标角色时绑定当前角色）
  - 推荐值：`current-character`

说明：

- 换麦识别由前端/应用层通过简洁语法（`=`、`->`）解析，**不依赖关键词配置**。
- parser 可继续透传 `frontmatterRaw` 与最小 metadata，不强制内建该语义。

## 5. 角色与台词

### 5.1 角色声明与状态进入

行首的 `@` 后紧跟角色名，解析器即进入该角色的 **Character Context**。

- **语法**：`@<角色名>[<情绪/状态提示>]` 或 `@<角色名>【<情绪/状态提示>】`
- **多角色声明**：允许同一行多个角色标识（例如 `@peter @bobby [aside]`），按实现约定生成主角色与别名列表
- **吞噬规则**：一旦进入角色状态，后续的普通段落、空行、列表（`-`, `1.`）、引用块（`>`）均作为该角色的**台词内容 (Dialogue Content)**，直到遇到状态重置指令。

```markdown
@哈姆雷特
我有两个选择：

- 生
- 还是死
```

- **状态重置**：遇到 `---` 或 `#` 时，解析器退出角色状态，回到全局动作状态。

### 5.2 动作提示 (Inline Action)

台词内部的身体动作使用 `{}` 包裹。全角 `｛｝` 与半角 `{}` 等价。

- **语法**：`你给我出去！{指着大门}`

### 5.3 角色状态的显式退出

从角色状态回到全局动作状态，支持以下三种机制（按优先级从高到低）：

#### 5.3.1 显式角色退出指令（最高优先级）

独占一行的 `@@`（不包含其他文本）立即触发角色状态退出：

```markdown
@哈姆雷特
生存还是灭亡，这是一个问题。

@@

就在此时，歌队缓缓步入舞台。（⚠️ 已回到全局状态）
```

- **语义**：`@@` 作为一个显式的状态边界标记，不生成任何 AST 节点。
- **边界规则**：`@@` 前后可有空行，但不能是缩进行；行尾若有其他内容，应产出 warning。
- **使用场景**：当编剧希望在明确的位置切换到全局状态，而不依赖 `---` 或 `#` 结构标记时使用。

#### 5.3.2 结构级标记（中等优先级）

遇到根级别的 `---`（水平线）或 `#...`（标题）时，隐式退出当前角色状态：

```markdown
@冉阿让
我该如何逃脱？

---

就在此时，警长沙威出现了。
```

- **特性**：这些标记本身被保留在 AST 中作为文档结构元素。
- **注意**：如果 `---` 或 `#` 出现在嵌套上下文（如列表项或引用块内），则不触发角色状态退出（见第 12 章裁决二）。

#### 5.3.3 跨状态指令（特殊情况）

遇到以下指令时，角色状态隐式退出并进入新状态：

- **新角色声明**：`@新角色` 直接切换到新角色，无需显式 `@@`。
- **唱段开始**：遇到 `$$`，角色状态转入唱段状态（见第 7 章）。

#### 5.3.4 优先级与冲突处理

当多个退出信号在同一位置出现时，按以下优先级处理：

```
其他指令（@角色、$$） > 显式退出（@@） > 结构标记（---、#）
```

**示例**：

```markdown
@哈姆雷特
等等，我听到脚步声了！

@@
@歌队
（合唱开始...）
```

上例中，`@@` 的效果被 `@歌队` 覆盖；解析器直接将状态从哈姆雷特切换到歌队，无需生成独立的"角色状态已退出"节点。

## 6. 场景动作与结构分隔

要退出“角色状态”回到“全局状态”（撰写舞台动作），使用以下两种 CommonMark 结构级标记：

1. **Thematic Break (水平线)**：`---`, `***`, `___`
2. **ATX Headings (标题)**：`#`, `##`

水平线在 DraMark 中具有**重置为全局动作**的强语义。

```markdown
@冉阿让
我该何去何从？

---

就在此时，警长沙威猛地推开了大门。 （⚠️ 已回到全局状态，此句为场景动作）

@沙威
你跑不掉了！
```

## 7. 唱段与音乐容器

### 7.1 独立唱段 (Song Block / Musical Number)

由独占一行的 `$$` 开始，独占一行的 `$$` 结束。这标志着解析器进入**唱段模式 (Sung Context)**。
`$$` 是一个**宏观曲目容器**，在其内部，DraMark 的指令依然运作，但作用域被限制在音乐中：

- **遇到 `@角色名`**：不退出唱段，仅切换演唱者（支持多角色对唱/合唱）。
- **遇到 `---`**：不退出唱段，仅将表演状态切回全局动作（表示间奏或音乐伴奏下的舞台调度）。

```markdown
$$
@冉阿让
这颗充满爱的心！
---
% 音乐转为急促的弦乐，起义者们随着鼓点奔跑。

@马吕斯
听吧人民在歌唱！
$$
```

### 7.1.1 唱段的显式退出

从唱段模式回到全局动作或角色状态，支持以下机制：

#### 显式唱段结束标记

使用独占一行的 `$$` 闭合唱段容器。这是唱段最显式、最推荐的退出方式：

```markdown
$$
@冉阿让
这颗充满爱的心！

@马吕斯
听吧人民在歌唱！
$$

就在这一刻，双方的目光在舞台中央相遇。（已回到全局状态）
```

#### 隐式穿透规则

以下指令可以隐式闭合 `$$` 容器（见第 12 章裁决三）：

- **根级别标题**：`#` 或 `##` 等级别的标题会隐式闭合当前唱段，并生成一个新的标题节点。

```markdown
$$
@冉阿让
这颗充满爱的心！

# 第二幕

在一个月后的夜晚...
$$
```

上例中，`# 第二幕` 自动闭合 `$$` 唱段并输出标题。

**注意**：隐式穿透仅在整行且非嵌套的情况下有效。在引用块或列表内的结构标记不触发穿透。

### 7.2 行内唱段 (Inline Song)

在同行内使用 `$<唱词>$`，用于对白与短促唱词的无缝衔接。

实现裁决（v0.1.1 起）：

- 在 `$$` 唱段上下文（`sung`）内，`$...$` **不解析为 `inline-song`**，并回退为普通文本。
- 该规则用于避免“唱段内嵌套行内唱段”的语义冲突；若确需显示 `$`，可直接按普通字符书写。

## 8. 技术提示标记

技术标记（Tech Cues）用于灯光 (LX)、音响 (SND) 等后台调度，与演员文本严格解耦。

- **行内标记**：`<<LX01 GO>>`。行内标记具有超越 HTML 解析的**词法抢占权 (Lexical Preemption)**，一旦匹配，内部文本作为普通 CommonMark 处理。
- **独立标记**：由 `<<<` 开始， `>>>` 结束的独占行或多行块。一旦匹配，内部文本作为普通 CommonMark 处理。

实现兼容说明（v0.1.1 起）：

- 当底层 CommonMark 词法把 `<<...>>` 临时拆分为 `text("<") + html("<...>") + text(">")` 时，解析器应在 AST 归一化阶段恢复为单一 `inline-tech-cue` 节点。

### 8.1 麦克风分配与换麦扩展（实验性，app/扩展层）

换麦能力当前定义为**应用层扩展**，不是 DraMark 核心语法强制项。对于 Parser，所有 `<<...>>` 均为普通技术标记；语义解析（换麦识别、着色、事件提取）由前端/应用层处理。

#### 输入形态

支持两种输入形态：

1. **行内技术标记**：`<<role:HM1->HM2>>`（双括号）
2. **行间技术标记**：`<<<role1:HM1, role2=HM2>>>`（三括号，可独占一行或多行）

#### 简洁语法

换麦指令通过**简洁语法**识别，无需配置关键词：

前端换麦识别层推荐支持以下简洁语法（等号与箭头等价）：

| 语法                | 含义                   | 角色来源            | 源麦来源   |
| ------------------- | ---------------------- | ------------------- | ---------- |
| `<<=HM2>>`          | 切到 HM2               | 当前 `@角色` 上下文 | 角色默认麦 |
| `<<角色=HM2>>`      | 角色切到 HM2           | 显式指定            | 角色默认麦 |
| `<<HM1->HM2>>`      | 从 HM1 切到 HM2        | 当前 `@角色` 上下文 | 显式指定   |
| `<<角色:HM1->HM2>>` | 角色从 HM1 切到 HM2    | 显式指定            | 显式指定   |
| `<<角色:HM1=HM2>>`  | 同上（等号与箭头混用） | 显式指定            | 显式指定   |

**歧义处理规则**：

- 若角色名与麦 ID 存在歧义，优先匹配角色名
- 若无法自动消歧，应产出 warning 要求显式指定

#### 推荐默认行为

- 在角色上下文中，若换麦指令省略目标角色，则默认作用于当前 `@角色`
- 非角色上下文下省略目标角色，应产出 warning（不阻断正文解析）
- v1 不定义 `@group` 直接换麦语法；组级批量换麦留待后续以显式 member 映射引入

#### 兼容性说明

- 简洁语法（`=`, `->`）不依赖任何 frontmatter 配置，前端直接解析
- 行内 `<<...>>` 与行间 `<<<...>>>` 技术标记语义一致，仅形式不同
- 非重名场景默认按角色 `name` 匹配；重名时建议补 `id` 做消歧

## 9. 注释

注释仅在源文件或特定的工作版渲染目标中可见。

- **行注释**：`% 注释内容`（附着于当前节点，常用于译配直译或注音）。
- **块注释**：`%%` 独占一行开始，`%%` 结束。

## 10. 译配与多语言模式

在 Frontmatter 配置 `translation.enabled: true` 后启用。该模式原生支持原/译文的非对称对齐。

### 10.1 原文与译文的配对 (Translation Pair)

使用等号加空格（`= `）开头的行，表示**原文 (Source Text)**。

**吞噬闭包规则**：
一个 `= ` 声明会持续吞噬后续的普通文本行（包括嵌套的列表、引用块等）作为**译文 (Target Text)**，并将其打包为一组 `TranslationPair`。吞噬会一直持续，直到遇到以下情况之一：

1. 下一个 `= `（下一句原文开始）
2. 下一个 `@`（切换角色）
3. **根级别的** `---` 或 `#`（切回全局动作）
4. `$$`（唱段边界）

### 10.2 译配状态的显式退出

从译配对接状态回到普通文本模式，有两种机制：

#### 显式译配退出指令

独占一行的 `=`（不包含任何原文内容）作为**译配显式退出标记**：

```markdown
@冉阿让
= Who am I?
我是谁？
= Can I conceal myself?
我能否隐瞒真相？

=

（无需再配对，直接回到普通对白模式）

但我的良心永不允许。
```

- **效果**：关闭当前的译配对接流程，后续普通文本被解析为普通对白（不再期待译文）。
- **边界**：独占一行的 `=` 应无前缀缩进且行尾无其他内容。

#### 隐式退出触发条件

以下条件会自动闭合当前的译配对接状态：

1. **新的 `= ` 声明**：开启下一对原/译文对
2. **角色切换**：`@新角色` 重置译配上下文
3. **结构边界**：根级别 `---` 或 `#` 切回全局状态
4. **容器边界**：`$$` 进入唱段（唱段内可重新开始译配）
5. **EOF**：文档结束

```markdown
@冉阿让
= Who am I?
我是谁？

=

就在此时，我明白了。

---

@沙威
你不能逃脱。
```

上例中，独占一行的 `=` 仅关闭译配对接状态，并未退出角色状态，因此“就在此时，我明白了。”仍是冉阿让的普通对白；随后的 `---` 隐式退出角色状态回到全局动作；之后的 `@沙威` 正常声明新角色并进入沙威的语境。

## 11. 转义字符

支持使用反斜杠 `\` 转义 DraMark 功能字符。
合法的转义序列为：`\@`, `\$`, `\{`, `\}`, `\%`, `\<`, `\=`, `\>`。

## 12. 解析器实现指引与边缘情况裁决 (Edge Cases)

在实现 DraMark 的 Lexer/Parser 时，必须严格遵循以下 8 条裁决，以消除歧义：

### 裁决一：Frontmatter 配置块的预处理豁免权 (Phase 0 Priority)

虽然 `---` 用于触发状态重置，但**文档首部的 Frontmatter 边界符 `---` 具有第零优先级**。解析器在预处理阶段消耗掉前两个独占行的 `---`，它们**绝对不触发**任何 DraMark 状态重置逻辑。

### 裁决二：状态切换的“容器隔离原则” (Container Isolation)

`---` 和 `#` 具有状态重置和吞噬截断的能力，但**该能力仅在文档根级别（Root-level）有效**。

- 如果 `---` 或 `#` 出现在 CommonMark 的嵌套容器（如 `>` 引用块、`-` 列表项）内部，它们受容器隔离保护，**不触发** DraMark 的状态重置或译配截断。

### 裁决三：唱段容器的隐式穿透 (Song Container Breakout)

结构级标题（`#`, `##`）具备强制穿透 `$$` 容器的最高优先级。如果在 `$$` 内部遇到根级别标题，解析器将隐式闭合当前的 `$$` 唱段，暴露出标题。

### 裁决四：译配目标的数据类型 (Target as Block List)

在 AST 中，`TranslationPair` 的 `target` 属性不是单纯的纯文本字符串，而是 **Array of Blocks（块级节点数组）**。这意味着译文可以完美包含加粗、列表、多段落等格式。

### 裁决五：百分号 `%` 的防误伤词法 (Strict Comment Lexing)

为防止 `%` 误伤日常文本（如“利润下降了 20%”）：

- **行首注释**：若 `%` 是该行的第一个非空字符，整行为注释。
- **行尾/行内注释**：若 `%` 出现文本后，**其左侧必须至少包含一个空白字符**（即 ` %`），才能被识别为注释的起点。紧跟在非空白字符后的 `%` 视为普通文本。

### 裁决六：行内技术标记严禁跨行 (No Multi-line Leak)

行内标记 `<< ... >>` 内部**不允许包含换行符（`\n`）**。
_容错机制_：如果当前行扫描到了 `<<` 但行尾前未找到匹配的 `>>`，则放弃解析为技术标记，将其降级为普通文本或移交底层的 HTML 解析器。

_实现补充_：若底层词法阶段出现 `text("<") + html("<...>") + text(">")` 的拆分形态，归一化时仍应尝试恢复为 `inline-tech-cue`，并继续执行“不得跨行”约束。

### 裁决七：显式退出指令的作用域隔离 (Exit Scope Isolation)

显式退出指令（`@@` 和单行 `=`）仅在**文档根级别**触发状态退出。
若这些指令出现在 CommonMark 嵌套容器内（如列表项、引用块等），它们失效，作为普通文本处理：

```markdown
@角色
这是台词。

- 列表项 1
  @@ （此 @@ 无效，仅作为列表项内容）
- 列表项 2

@@ （此 @@ 有效，处于根级别）

现在回到全局状态。
```

### 裁决八：四层退出优先级与状态转移 (Exit Priority & State Transition)

当根级行可被多个状态切换规则命中时，采用“单行单决策”原则：该行仅应用最高优先级规则。跨行输入按行序依次应用，后续行可继续触发新的状态迁移。优先级如下（从高到低）：

1. **新角色宣言** (`@新角色`)：直接切换角色上下文
2. **新容器进入** (`$$`)：场景切换到唱段上下文
3. **显式退出指令** (`@@` 或单行 `=`)：推出当前上下文回到父级
4. **结构标记** (`---`、`#`)：触发隐式状态重置

补充约束：单行 `=` 仅在“译配对接上下文”中具有退出语义；否则按普通文本处理。

**示例**：

```markdown
@A
对白

@@
@B
（此处不是同一行冲突；而是先执行上一行 `@@` 退出，再由下一行 `@B` 触发新的角色切换）

---

@A
= Source
Target

=
@B
（此处同样属于跨行迁移：先由单行 `=` 退出译配，再由下一行 `@B` 切换角色）
```

## 13. 显式退出机制快速参考表

| 上下文      | 退出方法 | 指令          | 优先级 |
| ----------- | -------- | ------------- | ------ |
| 角色对白    | 显式退出 | `@@`          | 高     |
|             | 结构标记 | `---`/`#`     | 中     |
|             | 其他指令 | `@`/`$$`      | 高     |
| 唱段 (`$$`) | 配对结束 | `$$`          | 高     |
|             | 标题穿透 | `#`           | 特殊   |
| 译配对接    | 显式退出 | `=` (单行)    | 高     |
|             | 隐式截断 | `@`/`---`/`#` | 中     |

**通用规则**：

- 所有显式退出指令仅在**根级别**有效；嵌套容器内作为普通文本。
- 多个指令竞争时，按 [裁决八](#裁决八四层退出优先级与状态转移-exit-priority--state-transition) 优先级处理。
- 退出后的目标状态由当前容器层级决定（根文档或唱段容器）。

## 14. 附录 A：输入法与编辑器体验优化 (IME Tricks)

DraMark 的设计恪守 ASCII 字符底座，但强烈建议各大编辑器（如 VS Code 插件、Obsidian 插件）在应用层实现以下静默映射（IME Tricks），以提供极致的中文创作心流：

当侦测到用户在全角/中文输入法状态下连续键入以下符号时，应**自动替换**为 DraMark 标准标记：

1. **技术标记**：输入 `《《` 自动转为 `<<`，输入 `》》` 自动转为 `>>`。
2. **唱段标记**：输入 `￥￥`（Shift+4 连按）自动转为 `$$`。
3. **动作提示**：输入 `【【` 自动转为 `{`，输入 `】】` 自动转为 `}`。

---

_DraMark：专为戏剧工作者设计的无干扰语义写作语言。_
